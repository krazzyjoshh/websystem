[phases.setup]
nixPkgs = ["php74", "php74Extensions.pdo_mysql", "php74Extensions.mbstring", "php74Extensions.xml", "php74Extensions.curl", "php74Extensions.tokenizer", "php74Extensions.json", "nodejs-18_x"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "npm install"]

[phases.build]
cmds = ["npm run build", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
